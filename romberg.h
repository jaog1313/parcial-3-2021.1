#ifndef ROMBERG_H
#define ROMBERG_H
#include <functional>

using::function;
class romberg{
public:
	
private:
	double integrar(function<double(double)> f,
					double limite_inf,
					double limite_sup,
					int intervalos
					){
		double result(0.0f);
		return result;
	}
};
#endif
